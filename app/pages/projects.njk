{% extends "layouts.njk" %}

{% block meta %}
{% set current_page = "projects" %}
{# Navbar #}
{% import 'macros/nav-macro.njk' as nav %}
{{nav.active(current_page)}}
{# Vault #}
{% include 'partials/vault.njk' %}
{# Heading #}
{% import 'macros/heading.njk' as heading %}
{{heading.active(current_page)}}

{# Content #}
{% import 'macros/page-macro.njk' as page %}
{{page.active(current_page)}}

    {# To populate - cycle through using macros #}
    
    <section class="gallery">

        {# Spacer columns for large displays #}
        <div class="gallery__project gallery__project--hidden"></div>
        <div class="gallery__project gallery__project--hidden"></div>

        {# Dynamically add projects here #}
        <div class="gallery__project">1</div>
        <div class="gallery__project">2</div>
        <div class="gallery__project">3</div>
        <div class="gallery__project">4</div>
        <div class="gallery__project">5</div>
        <div class="gallery__project">6</div>
        <div class="gallery__project">7</div>
        <div class="gallery__project">8</div>
        <div class="gallery__project">9</div>
        <div class="gallery__project">10</div>
        <div class="gallery__project">11</div>
        <div class="gallery__project">12</div>
        <div class="gallery__project">13</div>
        <div class="gallery__project">14</div>
        <div class="gallery__project">15</div>
        <div class="gallery__project">16</div>
        
        {# Spacer columns for large displays #}
        <div class="gallery__project gallery__project--hidden"></div>
        <div class="gallery__project gallery__project--hidden"></div>

    </section>

</div> {# Ends page macro #}
{% endblock %}